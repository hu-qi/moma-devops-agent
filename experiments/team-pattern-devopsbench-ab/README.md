# Team Pattern DevOpsBench A/B

Manual live-model experiment for the governed Team Pattern candidate.

Comparison:

    baseline
      dynamic DevOps Leader -> Coding -> Review

    candidate
      same MoMA role models
      + same deterministic DevOpsBench case
      + validated candidate Swarm Skill mounted through OpenJiuwen skill_use rail

Primary target metric:

    runtime_clean_completion

Hard guardrail:

    task_success must not regress

The experiment uses only a synthetic creation approval. The real repeated-timeout Team Pattern proposal remains pending human approval. Even if the automated gate passes, the resulting promotion state is PENDING_HUMAN, never APPROVED.

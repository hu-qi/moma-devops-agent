# Swarm Skill Candidate

Live sandbox proof of the governed Team Pattern creation boundary.

The experiment intentionally uses a synthetic APPROVED decision. It does not approve the real proposal stored from repeated AgentTeam timeout evidence.

Flow:

    synthetic approved proposal
      -> JiuwenSwarm swarmskill-creator specification
      -> MoMA tool call emits multi-file bundle
      -> materialize in temporary sandbox
      -> official validate_swarmskill.py
      -> EvolutionCandidate
      -> SQLite audit store
      -> uploaded CI artifact

Production skills/ must remain unchanged.

Pinned external creator dependency:

    openJiuwen-ai/jiuwenswarm
    6ab1ed59a8c455cdb9dfe45db34a3a8d42c10d47
